<h1> List Tour </h1>

<div>
  <li>
    <% @tours.each do |tour| %>
      <%= tour.name %>
      <%= tour.price %>
      <%= tour.place %>
      <%= image_tag tour.picture.url if tour.picture? %>
    <% end %>
  </li>
</div>
